
验证码需要客户提供  
https://119.164.254.72:54491  
https://119.164.254.72:51234


# 2、慢sql查询

CREATE TABLE `lt114_r_c_e` (
  `session_id` bigint(20) NOT NULL,
  `start_time` datetime NOT NULL,
  `end_time` datetime NOT NULL,
  `duration` int(11) DEFAULT NULL,
  `channel_type` int(11) DEFAULT NULL,
  `call_type` int(11) DEFAULT NULL,
  `local_url` varchar(32) DEFAULT NULL,
  `remote_url` varchar(32) DEFAULT NULL,
  `area_id` varchar(255) DEFAULT NULL,
  `end_type` int(11) DEFAULT NULL,
  PRIMARY KEY (`session_id`,`start_time`,`end_time`) USING BTREE,
  KEY `idx_start_time` (`start_time`) USING BTREE,
  KEY `idx_end_time` (`end_time`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

