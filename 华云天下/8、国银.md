

【国银金租】亲爱的：yansw01,您好！ 您的账户已经创建完毕。 您可以使用以下凭据进行登录： 用户名:yansw01 密码:4DYdif#n

|       |                                                                                                               | 完成情况       |
| ----- | ------------------------------------------------------------------------------------------------------------- | ---------- |
| 视图    | 默认视图只保留：我发的全部工单、我的未处理工单、我的已处理工单、我的关闭工单、全部工单、全部未处理、全部处理中、全部关闭工单。把所有关闭两个字，改为结单                                  | √          |
| 列表    | 添加申请休假按钮，点击后进行确认，确认后改为休假状态，不会再主动派发工单                                                                          | √          |
| 批量建单  | 选择批量建单的时候，需要修改工单模版，字段同列表字段。                                                                                   | √ 提供模板id接口 |
| 筛选    | 联系方式旁边加上身份证号筛选。其他字段的筛选条件放在更多中                                                                                 | √          |
| 详情    | 添加合同详情，可以切换展示合同详情                                                                                             | √          |
| 详情    | 添加跟进记录的时候，可以更新当前，也可以新增跟进记录。新增之后就是把填写的字段复制出来一份。有新复制的内容后，之前的记录不允许修改（工单节点表单可以多次填写，可以复制一份单独填写，保留原来的内容） -- 新写个表做这个 | √          |
| 详情    | 转出确认。如果查看人家是当前处理人，并且有人向他发起工单转出，转出还在有效期内，则展示转出按钮。其他情况不展示。点击弹窗确认框。（约等我我们的转办，只是发起人不同。确认框的30秒要倒计时）操作记录同步展示转出信息    | ×          |
| 详情    | 关闭改为结单。可以输入结单备注。并修改是否已结单状态                                                                                    | √          |
| 处理人设置 | 工单流程设置下，新增一个子菜单，可以添加不同类型对应的解决方和对接角色。一个相同的类型可以添加多个。不区分低于其他设置。                                                  | √          |

1、工单详情跟进记录短信内容是否要回显
2、工单管理：客户电话和来电号码、风险类型

工单列表
工单： 工单ID、接听人、是否完结、创建时间
客户：申请编号、合同编号、客户姓名、渠道名称
小结：问题类型、问题主题






## 8.1、跟进记录

## 8.2、处理人设置

## 8.3、工单列表

## 8.4、客户资料同步（身份证号）

```sql
ALTER TABLE `bdxm_contact` 
ADD COLUMN `id_number` varchar(100) DEFAULT NULL COMMENT '身份证号码';
```

## 8.5、升级


## 8.6、todo
1、工单转出确认
2、工单批量导入
3、关单列表和筛选字段 √
4、合同详情数据




我这边国银的需求都做完了，还有那个短信模板和发送的接口也对完了，这周先把他们提供的模板编号都配上测试一下。另外工单批量导入还有些问题需要处理一下。剩下的就是看测试的提的bug。

国银金租
1、测试环境把所有短信模板编号都配上测一下 0.5
2、工单批量导入报错的问题需要处理一下 1
3、测试bug处理 



# 1、目标检测评价指标

## 1.1、交并比

衡量预测框与真实框的重叠程度

```text
IoU = 预测框 ∩ 真实框的面积 / 预测框 ∪ 真实框的面积
```

- **阈值设定**：通常设为0.5（COCO数据集用0.5:0.95的多阈值）
- 如果IoU ≥ 阈值 → 被认为是正确检测（TP候选）


## 1.2、混淆矩阵的四个基本量

对于每个类别：

- **TP（真阳性）**：正确检测（IoU ≥ 阈值且分类正确）
- **FP（假阳性）**：错误检测（IoU < 阈值，或分类错误，或重复检测）
- **FN（假阴性）**：漏检（真实物体未被检测到）
- **TN（真阴性）**：背景正确识别（目标检测中一般不直接计算）

## 1.3、精确率（precision）与召回率（recall）

- **精确率 P = TP / (TP + FP)** - “找得准不准”
- **召回率 R = TP / (TP + FN)** - “找得全不全”

## 1.4、P-R曲线（精确率-召回率曲线）

通过改变**置信度阈值**得到的一系列(P, R)点绘制的曲线：

- 阈值高 → 精确率高、召回率低（保守）
- 阈值低 → 精确率低、召回率高（激进）

## 1.5、mAP（平均精度）

1. **AP（平均精度）**
- 对单个类别，**P-R曲线下的面积**
- 计算方式（两种主流）：
	- **VOC 2007**：在召回率0-1之间等间隔取11个点，计算精确率平均值
	- **VOC 2012+ / COCO**：对所有不同的召回率点，计算精确率的插值平均值


2. **mAP（mean Average Precision）**
- **所有类别的AP的平均值**
- YOLO论文和报告中**最常引用的指标**

3. **mAP的不同变体**
```text
mAP@0.5   : IoU阈值固定为0.5时的mAP
mAP@0.5:0.95 : IoU阈值从0.5到0.95，步长0.05，取10个阈值的平均值（COCO标准）
```


| **模块编号** | **模块名称**                 | **核心工作内容**                                                                                        | **报价（元）** | **备注**          |
| ---- | -------------------- | --------------------------------------------------------------------------------------------- | ----- | ----------- |
| 1    | **焊缝缺陷检测模型训练与API服务** | 1. 数据预处理与增强  <br>2. 对400张原始图片进行专业标注  <br>3. 模型选型、训练  <br>4. 开发并部署高性能推理API接口  <br>5. 提供API调用文档 | 8000  | 不包含后续模型训练费用 |
| 2    | **Web端推理演示系统（含UI）**  | 1. 设计简洁直观的用户界面  <br>2. 实现图片上传、实时推理功能  <br>3. 可视化展示缺陷位置与类别  <br>4. 实现检测报告生成与保存                 | 12000 |             |
| 3    | **自助式模型训练平台**        | 1. 用户管理、数据管理后台  <br>2. 实现用户自行上传、标注数据的功能  <br>3. 集成训练任务调度与监控模块  <br>4. 系统安全性设计与保障              | 16000 |             |
